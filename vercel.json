{
  "buildCommand": "npm install --prefix app && npm run build --prefix app",
  "outputDirectory": "app/dist",
  "installCommand": "echo 'Installing in app directory...'",
  "functions": {
    "api/**/*.js": {
      "memory": 128,
      "maxDuration": 10
    }
  }
}
